<Page
    x:Class="Bookmark_Manager.AddBookmarkPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bookmark_Manager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="19,12,0,26.5">
            <TextBlock Text="BOOKMARK MANAGER" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="PageHeader" Text="add bookmark" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="19,0,19,0">
            <TextBlock Text="Bookmark URL" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,9.5"/>
            <TextBox x:Name="UrlTextBox" InputScope="Url" Margin="0,0,0,20"/>

            <TextBlock Text="Category" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,9.5"/>
            <ComboBox x:Name="CategoryComboBox" Margin="0,0,0,20" HorizontalAlignment="Stretch">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Button x:Name="SaveButton" Content="Save" HorizontalAlignment="Stretch" Click="SaveButton_Click"/>
            <ProgressRing x:Name="LoadingProgress" Margin="0,15,0,0" Width="30" Height="30" IsActive="False"/>
        </StackPanel>
    </Grid>
</Page>
