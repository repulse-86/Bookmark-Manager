<Page
    x:Class="Bookmark_Manager.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bookmark_Manager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="19,12,0,26.5">
            <TextBlock Text="BOOKMARK MANAGER" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="about" Style="{ThemeResource HeaderTextBlockStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="19,0,19,0">
            <TextBlock x:Name="VersionTextBlock" Text="Version"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Margin="0,0,0,10" Opacity="0.6"/>
            <TextBlock Text="Bookmark Manager for Windows Phone 8.1" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,10"/>
            <TextBlock Text="Save, organize, and quickly open your favorite links." Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,20"/>
            <TextBlock Text="support the project" Style="{ThemeResource BodyTextBlockStyle}" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock Text="If you find this app useful, consider supporting development on Ko-fi:" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,15"/>
            <Button Content="Support on Ko-fi" HorizontalAlignment="Stretch" Click="SupportButton_Click" Height="60" FontSize="16"/>
            <TextBlock Text="Opens ko-fi.com/nextcall in your browser." Style="{ThemeResource BodyTextBlockStyle}" FontSize="12" Margin="0,10,0,0" Opacity="0.6"/>
        </StackPanel>
    </Grid>
</Page>
