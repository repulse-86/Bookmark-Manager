<Page
    x:Class="Bookmark_Manager.AddCategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bookmark_Manager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="19,12,0,26.5">
            <TextBlock Text="BOOKMARK MANAGER" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="PageHeader" Text="add category" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="19,0,19,0">
            <TextBlock Text="Category Name" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,9.5" />
            <TextBox x:Name="CategoryNameTextBox" Margin="0,0,0,20"/>
            <Button Content="Save" HorizontalAlignment="Stretch" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
