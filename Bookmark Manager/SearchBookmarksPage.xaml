<Page
    x:Class="Bookmark_Manager.SearchBookmarksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bookmark_Manager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="19,12,0,10">
            <TextBlock Text="BOOKMARK MANAGER" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="search bookmarks" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="19,0,19,15">
            <TextBox x:Name="SearchTextBox" InputScope="Search" KeyDown="SearchTextBox_KeyDown" PlaceholderText="enter search term..." Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
            <Button x:Name="SearchButton" Content="Search" Click="SearchButton_Click" HorizontalAlignment="Stretch"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="19,0,19,0">
            <GridView x:Name="SearchResultsGridView"
                      IsItemClickEnabled="True"
                      ItemClick="SearchResultsGridView_ItemClick"
                      SizeChanged="SearchResultsGridView_SizeChanged"
                      Padding="0"
                      Margin="0">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="{ThemeResource PhoneAccentBrush}"
                              Holding="BookmarkGrid_Holding">
                            <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Opacity="0.35" />

                            <Grid Margin="8" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Title}" Grid.Row="0" FontWeight="SemiBold" FontSize="14" MaxLines="2" TextWrapping="Wrap" Foreground="White" LineStackingStrategy="MaxHeight"/>
                                <TextBlock Text="{Binding Snippet}" Grid.Row="1" FontSize="10" Margin="0,4,0,0" TextWrapping="Wrap" Foreground="White" Opacity="0.8" LineStackingStrategy="MaxHeight"/>
                            </Grid>

                            <Border VerticalAlignment="Bottom" Background="#AA000000" Padding="6,4">
                                <TextBlock Text="{Binding Url}" FontSize="11" Foreground="White" TextTrimming="CharacterEllipsis"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </Grid>
    </Grid>
</Page>
